@model DoAnLapTrinhWeb.Model.Category

@{
    ViewData["Title"] = "Details";
}

<div>
    <h4>Category</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dd>
            <img style="max-width: 100%" src="@Model.ImageUrl" />
        </dd>
    </dl>
</div>
<div>
    <a class="btn btn-secondary" asp-action="Edit" asp-route-id="@Model?.CategoryId">Edit</a>
    <a class="btn btn-secondary" asp-action="Index">Back to List</a>
</div>
